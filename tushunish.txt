/* Git */
1. cd HududGaz
2. git init
3. git add .
4. git commit -m "Birinchi"

// Mahalliy
1. git add (Fayl nomi)
2. git commit -m "Izoh"

// Masofaviyga ulash
1. git branch -M main  -- joriy filialni o'rniga "main" ni yaratadi
2. git remote add origin https://github.com/Anvar-Avenger/HududGaz.git
4. git push -u origin main -- main ga yuklash

// Masofaviy ishlash
1. git pull
2. git commit -m "Izoh" --
3. git push -u remote main -- ixtiyoriy filialga

/* Git */
git status -- git dagi fayllar holati
git remote -- masofaviy serverga ulanganini tekshirish
git config


/* Laravelda mavjud jadvalga ustun qo'shish */
php artisan make:migration add_column_to_user --table="users"  //
